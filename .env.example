# Chave secreta da Stripe (Dashboard → Developers → API keys)
# Usada apenas pela função serverless api/create-checkout-session.ts
# Em produção define-se no Vercel (Project → Settings → Environment Variables);
# localmente, copiar este ficheiro para .env e preencher (usado por `vercel dev`)
STRIPE_SECRET_KEY=sk_test_xxx
