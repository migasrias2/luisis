# Chave secreta da Stripe (Dashboard → Developers → API keys)
# Usada apenas pela função serverless netlify/functions/create-checkout-session.mts
# Em produção define-se no Netlify (Site configuration → Environment variables);
# localmente, copiar este ficheiro para .env e preencher (usado por `netlify dev`)
STRIPE_SECRET_KEY=sk_test_xxx
